My manual on howto use git to update the sourcecode of this project.
(it has no use to try this yourself, since you do not have my password)


UPLOAD CODE:
git commit -a
git push googlecode master:master

SETUP:
git remote add googlecode https://renze%40rnplus.nl@code.google.com/p/openliveview/
git commit -m "<name>"
